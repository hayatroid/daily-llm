---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { processConversation } from '../../utils/contentProcessor.js';

export async function getStaticPaths() {
  // Get all conversation files (exclude summaries)
  const conversations = await Astro.glob('../../../content/**/*.md');
  const conversationFiles = conversations.filter(file => 
    !file.file.endsWith('summary.md')
  );
  
  return conversationFiles.map((conversation) => {
    const pathParts = conversation.file.split('/');
    const date = pathParts[pathParts.length - 2]; // Get date from path
    const filename = pathParts[pathParts.length - 1].replace('.md', ''); // Remove .md
    
    return {
      params: { 
        date: date,
        conversation: filename
      },
      props: { conversation }
    };
  });
}

const { date, conversation: conversationSlug } = Astro.params;
const { conversation } = Astro.props;
const { frontmatter, Content, rawContent } = conversation;

// Process the conversation for metadata
const processed = processConversation(rawContent());

const pageTitle = `${frontmatter.title} - ${date}`;
---

<BaseLayout title={pageTitle}>
  <div class="main-content">
    <nav class="breadcrumb-nav">
      <a href="/" class="breadcrumb-link">🏠 Home</a>
      <span class="breadcrumb-separator">{'>'}</span>
      <a href={`/${date}/`} class="breadcrumb-link">📅 {date}</a>
      <span class="breadcrumb-separator">{'>'}</span>
      <span class="breadcrumb-current">💬 {frontmatter.title}</span>
    </nav>

    <header class="conversation-header">
      <div class="header-main">
        <h1 class="conversation-title">🤖 {frontmatter.title}</h1>
        <div class="conversation-stats">
          <span class="stat">⏰ {frontmatter.time}</span>
          <span class="stat">🔄 {processed.exchangeCount} exchanges</span>
          {processed.hasCode && <span class="stat">💻 Code included</span>}
        </div>
      </div>
      
      {frontmatter.tags && frontmatter.tags.length > 0 && (
        <div class="conversation-tags">
          {frontmatter.tags.map((tag: string) => (
            <span class="tag">#{tag}</span>
          ))}
        </div>
      )}
    </header>

    <article class="conversation-content">
      <Content />
    </article>

    <footer class="conversation-footer">
      <div class="navigation-actions">
        <a href={`/${date}/`} class="nav-btn secondary">
          ← Back to {date}
        </a>
        <a href="/" class="nav-btn primary">
          🏠 All Days
        </a>
      </div>
    </footer>
  </div>
</BaseLayout>

<style>
  .breadcrumb-nav {
    margin-bottom: 2rem;
    padding: 0.75rem 1rem;
    background: rgba(0, 255, 0, 0.05);
    border-radius: 4px;
    border: 1px solid var(--term-dim);
  }

  .breadcrumb-link {
    color: var(--term-amber);
    text-decoration: none;
    font-size: 0.9rem;
  }

  .breadcrumb-link:hover {
    color: var(--term-green);
    text-decoration: underline;
  }

  .breadcrumb-separator {
    color: var(--term-dim);
    margin: 0 0.5rem;
  }

  .breadcrumb-current {
    color: var(--term-text);
    font-weight: bold;
  }

  .conversation-header {
    border: 1px solid var(--term-green);
    border-radius: 4px;
    padding: 1.5rem;
    margin-bottom: 2rem;
    background: rgba(0, 255, 0, 0.02);
  }

  .conversation-title {
    color: var(--term-green);
    margin: 0 0 1rem 0;
    font-size: 1.5rem;
    font-weight: bold;
  }

  .conversation-stats {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    margin-bottom: 1rem;
  }

  .stat {
    color: var(--term-dim);
    font-size: 0.875rem;
    background: rgba(102, 102, 102, 0.1);
    padding: 0.25rem 0.5rem;
    border-radius: 3px;
  }

  .conversation-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .tag {
    color: var(--term-cyan);
    font-size: 0.8rem;
    background: rgba(0, 255, 255, 0.1);
    padding: 0.2rem 0.5rem;
    border-radius: 3px;
    border: 1px solid var(--term-cyan);
  }

  .conversation-content {
    border: 1px solid var(--term-gray);
    border-radius: 4px;
    padding: 2rem;
    margin-bottom: 2rem;
    background: rgba(255, 255, 255, 0.01);
    line-height: 1.7;
  }

  .conversation-content :global(h2) {
    color: var(--term-amber);
    font-size: 1.1rem;
    margin: 2rem 0 1rem 0;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid var(--term-dim);
  }

  .conversation-content :global(pre) {
    background: rgba(0, 255, 255, 0.05);
    border: 1px solid var(--term-cyan);
    border-radius: 4px;
    padding: 1rem;
    margin: 1rem 0;
    overflow-x: auto;
  }

  .conversation-content :global(code) {
    color: var(--term-cyan);
    background: rgba(0, 255, 255, 0.1);
    padding: 0.2em 0.4em;
    border-radius: 3px;
    font-size: 0.9em;
  }

  .conversation-content :global(pre code) {
    background: none;
    padding: 0;
  }

  .conversation-footer {
    border-top: 1px solid var(--term-gray);
    padding-top: 1.5rem;
  }

  .navigation-actions {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 1rem;
  }

  .nav-btn {
    display: inline-flex;
    align-items: center;
    padding: 0.75rem 1rem;
    border-radius: 4px;
    text-decoration: none;
    font-size: 0.9rem;
    transition: all 0.2s ease;
  }

  .nav-btn.primary {
    background: var(--term-green);
    color: var(--term-bg);
    border: 1px solid var(--term-green);
  }

  .nav-btn.primary:hover {
    background: var(--term-amber);
    border-color: var(--term-amber);
  }

  .nav-btn.secondary {
    background: transparent;
    color: var(--term-amber);
    border: 1px solid var(--term-amber);
  }

  .nav-btn.secondary:hover {
    background: var(--term-amber);
    color: var(--term-bg);
  }

  /* Responsive design */
  @media (max-width: 767px) {
    .conversation-header {
      padding: 1rem;
    }

    .conversation-content {
      padding: 1.25rem;
    }

    .navigation-actions {
      flex-direction: column;
      gap: 0.75rem;
    }

    .nav-btn {
      width: 100%;
      justify-content: center;
    }

    .breadcrumb-nav {
      padding: 0.5rem;
      font-size: 0.8rem;
    }
  }
</style>